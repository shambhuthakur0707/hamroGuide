### Copy to `.env` and fill values before running the server
# MongoDB connection string. If omitted, the app will try a local MongoDB at `mongodb://127.0.0.1:27017/hamroGuide`
MONGO_URI=

# Port the server will listen on (default 5000)
PORT=5000
# Copy this file to .env in the same folder and fill the values before running the server.
# Example MongoDB connection string (replace with your credentials or use local MongoDB):
MONGO_URI=mongodb://username:password@host:port/dbname

# Port the server will listen on (optional, defaults to 5000)
PORT=5000
# Example environment variables for hamro-backend
# Copy to `.env` and set real values before running the server.

# Remote MongoDB connection (example for Atlas). Replace placeholders with real credentials.
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/hamro?retryWrites=true&w=majority

# Local fallback (if you run MongoDB locally, the code will try this automatically)
# local example: mongodb://127.0.0.1:27017/hamro

PORT=5000
### Example .env for hamro-backend
# Replace the MONGO_URI value with your MongoDB connection string.
# For a local MongoDB instance the default fallback is used automatically:
# mongodb://127.0.0.1:27017/hamroGuide

MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/hamroGuide?retryWrites=true&w=majority

# Optional: PORT=5000
